---
phase: 02-create-wizard
plan: 02
type: execute
---

<objective>
Build Step 1 of the wizard: property data input with address, price, specs, and neighborhood POI.

Purpose: Capture property information needed for script generation and video context.
Output: Complete property data form that validates and saves to wizard state.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context:
@.planning/phases/02-create-wizard/02-01-SUMMARY.md

# Database schema reference:
@supabase/migrations/001_initial_schema.sql

# UI components:
@src/components/ui/input.tsx
@src/components/ui/label.tsx
@src/components/ui/card.tsx
@src/lib/wizard/types.ts

**Constraining decisions:**
- Property types match listings table enum
- neighborhood_pois stored as JSONB array
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property data form component</name>
  <files>src/components/wizard/steps/property-data-step.tsx, src/components/ui/select.tsx</files>
  <action>
Create PropertyDataStep form component:

Fields (matching listings table):
- address (required, text)
- city, state, zip (text inputs in row)
- price (number, formatted as currency)
- bedrooms (number), bathrooms (decimal), sqft (number)
- property_type (select: single_family, condo, townhouse, multi_family, land, commercial, other)
- description (optional textarea)

Add shadcn Select component (npx shadcn@latest add select).
Use react-hook-form for form state with Zod validation.
On valid submit: call setPropertyData from useWizard context.

Luxury styling: gold focus rings, dark inputs, proper spacing.
  </action>
  <verify>npm run build succeeds, form renders with all fields</verify>
  <done>Property form captures all fields, validates, and updates wizard state</done>
</task>

<task type="auto">
  <name>Task 2: Create neighborhood POI input</name>
  <files>src/components/wizard/steps/property-data-step.tsx (extend)</files>
  <action>
Add neighborhood POI section to PropertyDataStep:

- Section header: "Nearby Points of Interest"
- Tag-style input: user types POI name, presses Enter to add
- Display as removable chips/badges (X to remove)
- Examples placeholder: "Whole Foods, Central Park, Metro Station"
- Max 10 POIs
- Store in wizard state as string array

Use Badge component if exists, or create simple chip styling.
POIs will be used in script generation to mention neighborhood features.
  </action>
  <verify>Can add/remove POI tags, state updates correctly</verify>
  <done>POI input allows adding/removing tags, persists to wizard state</done>
</task>

<task type="auto">
  <name>Task 3: Wire up step navigation</name>
  <files>src/app/(protected)/create/page.tsx</files>
  <action>
Update /create page to render PropertyDataStep for step 1:

- Import PropertyDataStep component
- Render when currentStep === WizardStep.DATA
- Next button enables only when property form is valid
- On Next: save form data to wizard context, advance to step 2

Form validation state should flow up to enable/disable Next button.
Consider using a ref or callback prop for validation state.
  </action>
  <verify>Complete property form, click Next, verify step advances</verify>
  <done>Step 1 → Step 2 navigation works with validated data</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] Property form has all required fields with validation
- [ ] POI input allows adding/removing tags
- [ ] Next button disabled until form valid
- [ ] Data persists in wizard context after step change
</verification>

<success_criteria>

- All 3 tasks completed
- All verification checks pass
- Form matches luxury aesthetic
- Validation prevents empty required fields
</success_criteria>

<output>
After completion, create `.planning/phases/02-create-wizard/02-02-SUMMARY.md`
</output>
