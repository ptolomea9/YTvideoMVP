---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Next.js 15 project with Tailwind CSS and shadcn/ui component library.

Purpose: Establish the frontend foundation with proper project structure, styling system, and reusable components for the luxury video suite.
Output: Working Next.js 15 app with Tailwind configured, shadcn/ui initialized, and design tokens matching the luxury dark theme.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech stack (locked):**
- Next.js 15 (App Router)
- Tailwind CSS
- shadcn/ui

**Design direction:**
- Dark Mode 2.0: Deep Charcoal (#0F0F0F)
- Typography: Bodoni Moda (serif headings), Inter (functional text)
- Framer Motion for micro-interactions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in project root.

Accept defaults for:
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes
- src/ directory: Yes
- App Router: Yes
- Import alias: @/*

This creates the Next.js 15 skeleton with App Router and Tailwind pre-configured.
  </action>
  <verify>npm run dev starts without errors, localhost:3000 shows Next.js welcome page</verify>
  <done>Next.js 15 project running with TypeScript, Tailwind, and App Router</done>
</task>

<task type="auto">
  <name>Task 2: Configure shadcn/ui and luxury design tokens</name>
  <files>components.json, tailwind.config.ts, src/app/globals.css, src/lib/utils.ts</files>
  <action>
1. Initialize shadcn/ui: `npx shadcn@latest init`
   - Style: Default
   - Base color: Neutral (we'll customize)
   - CSS variables: Yes
   - React Server Components: Yes
   - Import alias for components: @/components
   - Import alias for utils: @/lib/utils

2. Update tailwind.config.ts with luxury theme colors:
   - background: #0F0F0F (Deep Charcoal)
   - foreground: #FAFAFA
   - primary: #D4AF37 (Gold accent)
   - muted: #1A1A1A
   - border: #2A2A2A

3. Update globals.css with:
   - Dark mode as default (no light mode toggle needed)
   - Import Bodoni Moda from Google Fonts for headings
   - Inter is already system default for body text
   - CSS custom properties for the color tokens

4. Install Framer Motion for future micro-interactions: `npm install framer-motion`

Do NOT add complex animations yet - just ensure the dependency is available.
  </action>
  <verify>npm run build succeeds, no Tailwind/CSS errors</verify>
  <done>shadcn/ui initialized, design tokens configured, Framer Motion installed</done>
</task>

<task type="auto">
  <name>Task 3: Add core shadcn/ui components and create app shell</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
1. Install essential shadcn/ui components:
   - `npx shadcn@latest add button`
   - `npx shadcn@latest add card`
   - `npx shadcn@latest add input`

2. Update src/app/layout.tsx:
   - Set page title: "Edge AI Luxury Video Suite"
   - Apply dark theme class to html element
   - Use Inter as base font (via next/font/google)
   - Add Bodoni Moda via next/font/google for headings (expose as CSS variable --font-heading)

3. Update src/app/page.tsx with minimal placeholder:
   - Simple centered message: "Edge AI Luxury Video Suite" in Bodoni Moda
   - Subtext: "Coming soon" in Inter
   - Apply the dark background (#0F0F0F)

Keep it minimal - this is just to verify the styling pipeline works.
  </action>
  <verify>npm run dev shows styled placeholder page with correct fonts and dark background</verify>
  <done>Core UI components installed, app shell displays with luxury dark theme and custom fonts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Localhost shows dark-themed placeholder with Bodoni Moda heading
- [ ] shadcn/ui Button component renders correctly
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Next.js 15 project structure in place
- Tailwind configured with luxury dark theme tokens
- shadcn/ui initialized with core components
- Custom fonts (Bodoni Moda, Inter) loading correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
